#include "strings.h"
#include "io.h"

int main(int argc, char* argv[])
{
   int counter = 1;
   char buffer[4096];
   while (true)
    {
        int fd = lain::open_file()
        lain::write_file()
    }
}